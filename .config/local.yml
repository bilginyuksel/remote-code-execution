# container config
attachStdin: true
attachStdout: true
attachStderr: true
tty: true
cmd:
  - "bash"
image: "all-in-one-ubuntu:latest"

# container host config
mounts:
  - type: "bind"
    source: "/Users/bilginyuksel/workspace/codigician/remote-code-execution/target"
    target: "/target"